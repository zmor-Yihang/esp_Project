# Chip component CMakeLists.txt
# Add source files when they are created
# idf_component_register(SRCS "chip.c"
#                        INCLUDE_DIRS "."
#                        REQUIRES esp_common)
